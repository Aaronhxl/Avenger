	Function--函数式接口的介绍：
	1.JavaDoc中说明，提供一个参数，产生一个结果，这和数学中的函数是类似的
	
	Java lambda基本语法：
	1.Java中lambda表达式基本语法：（argument）->(body)
	2.一个lambda表达式可以有零个或多个参数
	3.参数的类型可以明确声明，也可以根据上下文来推断
	4.当只有一个参数，且其类型可推导时，圆括号（）可省略。例如：a->a*a

	高阶函数：
	如果一个函数接收一个函数作为参数，或者返回一个函数作为返回值，那么该函数
	就叫做高阶函数
	