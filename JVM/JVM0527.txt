1.当一个接口在初始化时，并不要求其父接口都完成初始化
  只有在真正使用到父接口的时候（如引用接口中所定义的常量时），才会初始化
2.其实和之前学的一样，一个原则，能在编译期间确定的绝不能在运行时确定
3.再次明确，初始化过程是第三阶段，前面的加载、连接，其中class对象是加载的时候
4.初始化过程是按照程序的声明顺序执行的
5.注意：接口中的变量默认是public static final，所以在初始化方面会有不同的特性